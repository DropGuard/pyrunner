{
  "name": "@dropguard/pyrunner",
  "version": "0.1.6",
  "description": "Lightweight cross-platform Python script scheduler via Bun and uv",
  "main": "bin.js",
  "type": "module",
  "bin": {
    "pyrunner": "bin.js"
  },
  "files": [
    "src",
    "templates",
    "bin.js"
  ],
  "private": false,
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile pyrunner.exe",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "cron-parser": "^5.5.0"
  }
}
